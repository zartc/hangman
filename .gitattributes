# Set the default behavior, in case one doesn't have core.autocrlf set.
*           text=auto

# These are explicitly linux files and should use lf
*.sh        text eol=lf
*.bash      text eol=lf
*.fish      text eol=lf

# These are explicitly windows files and should use crlf
*.bat       text eol=crlf
*.cmd       text eol=crlf
*.ps1       text eol=crlf

# These are truly binary and should not be modified.
*.png       binary
*.jpg       binary
