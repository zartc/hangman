package io.github.zartc.hangman.model.filter


import spock.lang.Specification

class AlphabeticLetterFilterTest extends Specification {
	/*@Shared
	LetterFilter letterFilter = new AlphabeticLetterFilter();

	def "upper '#letter' is acceptable"(char letter) {
		expect:
		letterFilter.isAcceptable(letter) == result;

		where:
		letter << ('A'..'Z')
		result = true
	}

	def "lower '#letter' is NOT acceptable"(char letter) {
		expect:
		letterFilter.isAcceptable(letter) == result;

		where:
		letter << ('a'..'z')
		result = false
	}

	def "digit '#letter' is not acceptable"(char letter) {
		expect:
		letterFilter.isAcceptable(letter) == result;

		where:
		letter << ('0'..'9')
		result = false
	}

	def "special chars '#letter' is not acceptable"(char letter) {
		expect:
		letterFilter.isAcceptable(letter) == result;

		where:
		letter || result
		' '    || false
		'.'    || false
		','    || false
		';'    || false
		':'    || false
		'-'    || false
		'_'    || false
		'/'    || false
		'`'    || false
		'{'    || false
		'@'    || false
		'['    || false
	}*/
}
