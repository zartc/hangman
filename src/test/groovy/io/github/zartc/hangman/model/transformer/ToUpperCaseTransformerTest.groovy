package io.github.zartc.hangman.model.transformer

import spock.lang.Specification

class ToUpperCaseTransformerTest extends Specification {
	/*@Shared
	LetterTransformer transformer = new ToUpperCaseTransformer();

	def "transform lower letters"(char letter, char result) {
		expect:
		transformer.transform(letter) == result;

		where:
		letter << ('a'..'z')
		result  << ('A'..'Z')
	}

	def "transform upper letters"(char letter, char result) {
		expect:
		transformer.transform(letter) == result;

		where:
		letter << ('A'..'Z')
		result  << ('A'..'Z')
	}*/
}
